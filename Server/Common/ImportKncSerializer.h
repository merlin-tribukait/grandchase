#pragma once

// Stub ImportKncSerializer header for compilation
// This would normally contain serialization imports

namespace KncSerializer
{
    template<typename T>
    void Import(const T& obj);
    
    template<typename T>
    void Export(T& obj);
}
